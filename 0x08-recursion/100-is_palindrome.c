#include "main.h"
/**
 * is_palindrome - checks wheter a string 
 * is palandiorm or not
 * @s: input parameter
 * Return: int( 1 or 0)
 */
int is_palindrome(char *s)
{
	if (*s == '\0')
		return (1);
	
}
