#include "main.h"
#include <stdio.h>
/**
 * main - entry point
 * @argc: input parameter
 * @argv: input parameter
 * Return: int
 */
int main(int argc, char *argv[])
{
	if (argv[0] != '\0')
	{
		printf("%d\n, (argc - 1));
	}
	return (0);
}
