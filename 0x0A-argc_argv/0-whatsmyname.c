#include "main.h"
#include <stdio.h>
/**
 * main - Entry point
 * @argc: input parameter
 * @argv: input parameter
 * Return: int
 */
int main(int __attribute__ ((unused)) argc, char *argv[])
{
	printf("%s\n", *argv);
	return (0);
}
