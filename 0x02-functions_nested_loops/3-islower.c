#include "main.h"
/**
 * _islower - checks wheter
 * a character is lowercase or not
 *
 * Retrun: 1 if a character is lowercase
 * Return: 0 if a character is not a lowercase
 */
int _islower(int c)
{
	if (c > 'a' && c < 'z')
		return 0;
	else
		return 0;
}
